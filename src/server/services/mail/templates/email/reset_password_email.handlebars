<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Password Email</title>
    <style>
      /* Ensure the full page uses flexbox to align elements */
      body,
      html {
        margin: 0;
        padding: 0;
        height: 100%;
        font-family: Arial, sans-serif;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
      }

      .container {
        padding: 100px 24px;
        max-width: 500px;
        margin: 0 auto;
        flex-grow: 1;
        text-align: center;
        color: #333;
      }

      .button {
        display: inline-block;
        width: 100%;
        padding: 12px;
        background-color: #5b52f9;
        color: white;
        text-align: center;
        text-decoration: none;
        font-size: 16px;
        border-radius: 4px;
        margin: 16px 0;
      }

      .footer {
        width: 100%; /* Ensure the footer takes the full width of the screen */
        text-align: center;
        color: #6b7280;
        font-size: 12px;
        padding-bottom: 80px;
        box-sizing: border-box;
      }

      .social-links {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 8px;
        margin-bottom: 16px;
      }

      .social-links a {
        display: block;
      }

      .social-links img {
        width: 30px;
        height: 30px;
      }

      .footer p {
        margin: 0;
        color: #6b7280;
      }

      .footer .description {
        width: 100%; 
        text-align: center; 
        margin: 8px auto;
        max-width: 100%; 
        padding: 0 16px; 
      }

      /* Center the logo */
      .logo-container {
        text-align: center;
        padding-top: 24px;
      }

      /* Add space between sections */
      h1 {
        font-size: 24px;
        font-weight: bold;
        margin-bottom: 16px;
        color: #333;
      }

      p {
        font-size: 14px;
        text-align: left;
        margin-bottom: 16px;
      }
    </style>
  </head>
  <body>
    <div class="logo-container">
      <img
        src="Title.svg"
        alt="Fradragsjakt Logo"
        width="152"
        height="32"
      />
    </div>

    <div class="container">
      <h1>Reset Password</h1>
      <p>
        Hi {{user}},<br />
        You recently requested to reset your password for your account. To
        complete the process, please click the button below:<br />
        The link expires in 48 hours.
      </p>

      <!-- Reset Password Button -->
      <a href="{{resetLink}}" class="button">Reset Password</a>

      <!-- Additional Info -->
      <p>
        If you did not request this change, please ignore this email. Your
        password will remain unchanged.<br />
        For security reasons, this link will expire in 24 hours.
      </p>
    </div>

    <!-- Footer -->
    <footer class="footer">
      <!-- Social Links -->
      <div class="social-links">
        <a href="{{facebookUrl}}">
          <img src="Facebook.svg" alt="Facebook" />
        </a>
        <a href="{{linkedinUrl}}">
          <img src="Linkedin.svg" alt="LinkedIn" />
        </a>
        <a href="{{instagramUrl}}">
          <img src="Instagram.svg" alt="Instagram" />
        </a>
      </div>

      <!-- Copyright -->
      <p style="text-align: center"> Copyright  &copy; 2024 Fradragsjakt</p>

      <!-- Description -->
      <p class="description">
        Fradragsjakt is a comprehensive tax management solution that assists
        freelancers, gig workers, independent contractors, family, and small
        business owners in tracking expenses and identifying tax write-offs.
      </p>
    </footer>
  </body>
</html>
